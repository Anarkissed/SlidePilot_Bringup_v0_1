#pragma once

// Deprecated compatibility header.
// Pin definitions live in ONE place only:
//   include/BoardPins.h
//
// If any old code includes "SlidePilotPins.h", it will still compile,
// but it will pull pins from BoardPins.h (single source of truth).

#include "BoardPins.h"